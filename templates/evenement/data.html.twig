{% extends 'base.html.twig' %}

{% block title %}
	QR code 
{% endblock %}



{% block body %}
	 <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header">
                        
                    </div>
                    <div class="card-body">
                   
                        
                        <p>Veuillez trouver ci-dessous le QR code pour accéder aux détails de votre evenement:</p>
                        <div class="text-center">
                            <img src="{{ qr_code_data_uri(evenement) }}" alt="QR code" class="img-fluid">   
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}