<form method="post" action="{{ path('app_offre_delete', {'idoffre': offre.idoffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.idoffre) }}">
    <button class="btn btn-light" style: 'font-size: 15px; padding: 15px 20px; margin-left: 10px; margin-top: 15px;margin-bottom: 15px;'> Supprimer</a></button>
	
              
</form>
