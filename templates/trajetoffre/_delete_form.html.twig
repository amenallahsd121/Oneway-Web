<form method="post" action="{{ path('app_trajetoffre_delete', {'idtrajetoffre': trajetoffre.idtrajetoffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trajetoffre.idtrajetoffre) }}">
    <button class="btn">Supprimer</button>
</form>
